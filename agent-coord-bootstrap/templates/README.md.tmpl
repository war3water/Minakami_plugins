# {{project_name}}

> _One-sentence pitch — what this project does and who it is for._

<!-- Optional badges — delete the line if not applicable.
[![status](https://img.shields.io/badge/status-WIP-yellow)](#)
[![license](https://img.shields.io/badge/license-MIT-blue)](LICENSE)
-->

<!-- Optional: screenshot, demo GIF, or asciinema embed. -->

## Why

_2–3 sentences. What problem does this solve, why does this exist, who
should care?_

## Features

_One line per real capability. List every capability that exists —
dormant or optional ones too, with how to enable them:_

- _Core capability — what it does._
- _Optional capability (off by default) — enable with `--flag` / config._

---

## Quick start

### Prerequisites

- _Language / runtime version_ (e.g. Python 3.11+, Node 20+, etc.)
- _External services_ (e.g. Docker, Postgres, an LLM API key)

### Install

```bash
git clone https://github.com/<your-org>/{{project_name}}.git
cd {{project_name}}
# language-specific install: pip install -e .  /  npm install  /  cargo build  /  go mod tidy
```

### Configure

_Delete this section if the project needs no environment configuration._

Copy `.env.example` to `.env` and fill in:

- `EXAMPLE_API_KEY` — _where to get it_
- `EXAMPLE_HOST` — _default; only override if you know why_

### Run

```bash
# the one canonical run command
```

You should see _what success looks like_ — e.g. a server listening on
`http://localhost:PORT`, a CLI prompt, an output file.

### Verify

```bash
# the canonical test / smoke command(s), e.g. pytest -q  /  npm test
```

These must pass before a change counts as done.

---

## Debug

_Placeholder examples — replace with real addresses, or delete rows that
don't apply._

| Component | Address | How to attach |
|---|---|---|
| _backend_ | `http://localhost:8000` | _VSCode `Run` → pick the matching profile in `.vscode/launch.json`_ |
| _frontend_ | `http://localhost:5173` | _open DevTools (F12); breakpoints in source tab_ |
| _tests_ | _n/a_ | _`pytest --pdb` / `npm test -- --inspect-brk` / etc._ |

Per-component VSCode launch profiles live in `.vscode/launch.json`.
Edit / add entries when you add a new entry point.

---

## Project structure

_Placeholder — adjust to the real tree._

```text
src/         Production code
tests/       Test suite
docs/        Long-form documentation (optional)
.vscode/     Debug + task configurations
```

---

## Roadmap

_Briefly: where the project sits in its lifecycle. Link to a roadmap
issue / project board if there is one._

## Contributing

_How can someone help? Open an issue, send a PR, join a discussion?_

## License

_e.g. MIT — see [LICENSE](LICENSE)._

---

_Coding agents: see [`AGENTS.md`](AGENTS.md)._
